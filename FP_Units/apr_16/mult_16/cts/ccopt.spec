###############################################################
#  Generated by:      Cadence Innovus 19.10-p002_1
#  OS:                Linux x86_64(Host ID en4192789rl)
#  Generated on:      Sat Mar 30 17:46:12 2024
#  Design:            mult16
#  Command:           create_ccopt_clock_tree_spec -filename ./cts/ccopt.spec
###############################################################
#-------------------------------------------------------------------------------
# Clock tree setup script - dialect: Innovus
#-------------------------------------------------------------------------------
#-------------------------------------------------------------------------------

if { [get_ccopt_clock_trees] != {} } {
  error {Cannot run clock tree spec: clock trees are already defined.}
}

namespace eval ::ccopt {}
namespace eval ::ccopt::ilm {}
set ::ccopt::ilm::ccoptSpecRestoreData {}
# Start by checking for unflattened ILMs.
# Will flatten if so and then check the db sync.
if { [catch {ccopt_check_and_flatten_ilms_no_restore}] } {
  return -code error
}
# cache the value of the restore command output by the ILM flattening code
set ::ccopt::ilm::ccoptSpecRestoreData $::ccopt::ilm::ccoptRestoreILMState

# Clocks present at pin clk
#   clk (period 900.0ps) in timing_config common([/home/aavyas1/research/Jonti_Transfers/SLM_RAS/FP_Units/synthesis_16/mult16/mult16.900.syn.sdc])
create_ccopt_clock_tree -name clk -source clk -no_skew_group
set_ccopt_property target_max_trans_sdc -delay_corner delayCorner_slow -early -clock_tree clk 0.1

set_ccopt_property target_max_trans_sdc -delay_corner delayCorner_slow -late -clock_tree clk 0.1
set_ccopt_property source_driver -clock_tree clk {INVx3_ASAP7_75t_R/A INVx3_ASAP7_75t_R/Y}
# Clock period setting for source pin of clk
set_ccopt_property clock_period -pin clk 900

# Skew group to balance non generated clock:clk in timing_config:common (sdc /home/aavyas1/research/Jonti_Transfers/SLM_RAS/FP_Units/synthesis_16/mult16/mult16.900.syn.sdc)
create_ccopt_skew_group -name clk/common -sources clk -auto_sinks
set_ccopt_property include_source_latency -skew_group clk/common true
set_ccopt_property extracted_from_clock_name -skew_group clk/common clk
set_ccopt_property extracted_from_constraint_mode_name -skew_group clk/common common
set_ccopt_property extracted_from_delay_corners -skew_group clk/common {delayCorner_slow delayCorner_fast}


check_ccopt_clock_tree_convergence
# Restore the ILM status if possible
if { [get_ccopt_property auto_design_state_for_ilms] == 0 } {
  if {$::ccopt::ilm::ccoptSpecRestoreData != {} } {
    eval $::ccopt::ilm::ccoptSpecRestoreData
  }
}

